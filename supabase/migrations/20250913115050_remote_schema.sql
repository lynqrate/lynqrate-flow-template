revoke delete on table "public"."ai_task_runs" from "anon";

revoke insert on table "public"."ai_task_runs" from "anon";

revoke references on table "public"."ai_task_runs" from "anon";

revoke select on table "public"."ai_task_runs" from "anon";

revoke trigger on table "public"."ai_task_runs" from "anon";

revoke truncate on table "public"."ai_task_runs" from "anon";

revoke update on table "public"."ai_task_runs" from "anon";

revoke delete on table "public"."ai_task_runs" from "authenticated";

revoke insert on table "public"."ai_task_runs" from "authenticated";

revoke references on table "public"."ai_task_runs" from "authenticated";

revoke select on table "public"."ai_task_runs" from "authenticated";

revoke trigger on table "public"."ai_task_runs" from "authenticated";

revoke truncate on table "public"."ai_task_runs" from "authenticated";

revoke update on table "public"."ai_task_runs" from "authenticated";

revoke delete on table "public"."ai_task_runs" from "service_role";

revoke insert on table "public"."ai_task_runs" from "service_role";

revoke references on table "public"."ai_task_runs" from "service_role";

revoke select on table "public"."ai_task_runs" from "service_role";

revoke trigger on table "public"."ai_task_runs" from "service_role";

revoke truncate on table "public"."ai_task_runs" from "service_role";

revoke update on table "public"."ai_task_runs" from "service_role";

revoke delete on table "public"."analysis_requests" from "anon";

revoke insert on table "public"."analysis_requests" from "anon";

revoke references on table "public"."analysis_requests" from "anon";

revoke select on table "public"."analysis_requests" from "anon";

revoke trigger on table "public"."analysis_requests" from "anon";

revoke truncate on table "public"."analysis_requests" from "anon";

revoke update on table "public"."analysis_requests" from "anon";

revoke delete on table "public"."analysis_requests" from "authenticated";

revoke insert on table "public"."analysis_requests" from "authenticated";

revoke references on table "public"."analysis_requests" from "authenticated";

revoke select on table "public"."analysis_requests" from "authenticated";

revoke trigger on table "public"."analysis_requests" from "authenticated";

revoke truncate on table "public"."analysis_requests" from "authenticated";

revoke update on table "public"."analysis_requests" from "authenticated";

revoke delete on table "public"."analysis_requests" from "service_role";

revoke insert on table "public"."analysis_requests" from "service_role";

revoke references on table "public"."analysis_requests" from "service_role";

revoke select on table "public"."analysis_requests" from "service_role";

revoke trigger on table "public"."analysis_requests" from "service_role";

revoke truncate on table "public"."analysis_requests" from "service_role";

revoke update on table "public"."analysis_requests" from "service_role";

revoke delete on table "public"."emotion_entries" from "anon";

revoke insert on table "public"."emotion_entries" from "anon";

revoke references on table "public"."emotion_entries" from "anon";

revoke select on table "public"."emotion_entries" from "anon";

revoke trigger on table "public"."emotion_entries" from "anon";

revoke truncate on table "public"."emotion_entries" from "anon";

revoke update on table "public"."emotion_entries" from "anon";

revoke delete on table "public"."emotion_entries" from "authenticated";

revoke insert on table "public"."emotion_entries" from "authenticated";

revoke references on table "public"."emotion_entries" from "authenticated";

revoke select on table "public"."emotion_entries" from "authenticated";

revoke trigger on table "public"."emotion_entries" from "authenticated";

revoke truncate on table "public"."emotion_entries" from "authenticated";

revoke update on table "public"."emotion_entries" from "authenticated";

revoke delete on table "public"."emotion_entries" from "service_role";

revoke insert on table "public"."emotion_entries" from "service_role";

revoke references on table "public"."emotion_entries" from "service_role";

revoke select on table "public"."emotion_entries" from "service_role";

revoke trigger on table "public"."emotion_entries" from "service_role";

revoke truncate on table "public"."emotion_entries" from "service_role";

revoke update on table "public"."emotion_entries" from "service_role";

revoke delete on table "public"."emotion_feedbacks" from "anon";

revoke insert on table "public"."emotion_feedbacks" from "anon";

revoke references on table "public"."emotion_feedbacks" from "anon";

revoke select on table "public"."emotion_feedbacks" from "anon";

revoke trigger on table "public"."emotion_feedbacks" from "anon";

revoke truncate on table "public"."emotion_feedbacks" from "anon";

revoke update on table "public"."emotion_feedbacks" from "anon";

revoke delete on table "public"."emotion_feedbacks" from "authenticated";

revoke insert on table "public"."emotion_feedbacks" from "authenticated";

revoke references on table "public"."emotion_feedbacks" from "authenticated";

revoke select on table "public"."emotion_feedbacks" from "authenticated";

revoke trigger on table "public"."emotion_feedbacks" from "authenticated";

revoke truncate on table "public"."emotion_feedbacks" from "authenticated";

revoke update on table "public"."emotion_feedbacks" from "authenticated";

revoke delete on table "public"."emotion_feedbacks" from "service_role";

revoke insert on table "public"."emotion_feedbacks" from "service_role";

revoke references on table "public"."emotion_feedbacks" from "service_role";

revoke select on table "public"."emotion_feedbacks" from "service_role";

revoke trigger on table "public"."emotion_feedbacks" from "service_role";

revoke truncate on table "public"."emotion_feedbacks" from "service_role";

revoke update on table "public"."emotion_feedbacks" from "service_role";

revoke delete on table "public"."one_time_email_deliveries" from "anon";

revoke insert on table "public"."one_time_email_deliveries" from "anon";

revoke references on table "public"."one_time_email_deliveries" from "anon";

revoke select on table "public"."one_time_email_deliveries" from "anon";

revoke trigger on table "public"."one_time_email_deliveries" from "anon";

revoke truncate on table "public"."one_time_email_deliveries" from "anon";

revoke update on table "public"."one_time_email_deliveries" from "anon";

revoke delete on table "public"."one_time_email_deliveries" from "authenticated";

revoke insert on table "public"."one_time_email_deliveries" from "authenticated";

revoke references on table "public"."one_time_email_deliveries" from "authenticated";

revoke select on table "public"."one_time_email_deliveries" from "authenticated";

revoke trigger on table "public"."one_time_email_deliveries" from "authenticated";

revoke truncate on table "public"."one_time_email_deliveries" from "authenticated";

revoke update on table "public"."one_time_email_deliveries" from "authenticated";

revoke delete on table "public"."one_time_email_deliveries" from "service_role";

revoke insert on table "public"."one_time_email_deliveries" from "service_role";

revoke references on table "public"."one_time_email_deliveries" from "service_role";

revoke select on table "public"."one_time_email_deliveries" from "service_role";

revoke trigger on table "public"."one_time_email_deliveries" from "service_role";

revoke truncate on table "public"."one_time_email_deliveries" from "service_role";

revoke update on table "public"."one_time_email_deliveries" from "service_role";

revoke delete on table "public"."pass_rollup_digests" from "anon";

revoke insert on table "public"."pass_rollup_digests" from "anon";

revoke references on table "public"."pass_rollup_digests" from "anon";

revoke select on table "public"."pass_rollup_digests" from "anon";

revoke trigger on table "public"."pass_rollup_digests" from "anon";

revoke truncate on table "public"."pass_rollup_digests" from "anon";

revoke update on table "public"."pass_rollup_digests" from "anon";

revoke delete on table "public"."pass_rollup_digests" from "authenticated";

revoke insert on table "public"."pass_rollup_digests" from "authenticated";

revoke references on table "public"."pass_rollup_digests" from "authenticated";

revoke select on table "public"."pass_rollup_digests" from "authenticated";

revoke trigger on table "public"."pass_rollup_digests" from "authenticated";

revoke truncate on table "public"."pass_rollup_digests" from "authenticated";

revoke update on table "public"."pass_rollup_digests" from "authenticated";

revoke delete on table "public"."pass_rollup_digests" from "service_role";

revoke insert on table "public"."pass_rollup_digests" from "service_role";

revoke references on table "public"."pass_rollup_digests" from "service_role";

revoke select on table "public"."pass_rollup_digests" from "service_role";

revoke trigger on table "public"."pass_rollup_digests" from "service_role";

revoke truncate on table "public"."pass_rollup_digests" from "service_role";

revoke update on table "public"."pass_rollup_digests" from "service_role";

revoke delete on table "public"."passes" from "anon";

revoke insert on table "public"."passes" from "anon";

revoke references on table "public"."passes" from "anon";

revoke select on table "public"."passes" from "anon";

revoke trigger on table "public"."passes" from "anon";

revoke truncate on table "public"."passes" from "anon";

revoke update on table "public"."passes" from "anon";

revoke delete on table "public"."passes" from "authenticated";

revoke insert on table "public"."passes" from "authenticated";

revoke references on table "public"."passes" from "authenticated";

revoke select on table "public"."passes" from "authenticated";

revoke trigger on table "public"."passes" from "authenticated";

revoke truncate on table "public"."passes" from "authenticated";

revoke update on table "public"."passes" from "authenticated";

revoke delete on table "public"."passes" from "service_role";

revoke insert on table "public"."passes" from "service_role";

revoke references on table "public"."passes" from "service_role";

revoke select on table "public"."passes" from "service_role";

revoke trigger on table "public"."passes" from "service_role";

revoke truncate on table "public"."passes" from "service_role";

revoke update on table "public"."passes" from "service_role";

revoke delete on table "public"."standard_emotions" from "anon";

revoke insert on table "public"."standard_emotions" from "anon";

revoke references on table "public"."standard_emotions" from "anon";

revoke select on table "public"."standard_emotions" from "anon";

revoke trigger on table "public"."standard_emotions" from "anon";

revoke truncate on table "public"."standard_emotions" from "anon";

revoke update on table "public"."standard_emotions" from "anon";

revoke delete on table "public"."standard_emotions" from "authenticated";

revoke insert on table "public"."standard_emotions" from "authenticated";

revoke references on table "public"."standard_emotions" from "authenticated";

revoke select on table "public"."standard_emotions" from "authenticated";

revoke trigger on table "public"."standard_emotions" from "authenticated";

revoke truncate on table "public"."standard_emotions" from "authenticated";

revoke update on table "public"."standard_emotions" from "authenticated";

revoke delete on table "public"."standard_emotions" from "service_role";

revoke insert on table "public"."standard_emotions" from "service_role";

revoke references on table "public"."standard_emotions" from "service_role";

revoke select on table "public"."standard_emotions" from "service_role";

revoke trigger on table "public"."standard_emotions" from "service_role";

revoke truncate on table "public"."standard_emotions" from "service_role";

revoke update on table "public"."standard_emotions" from "service_role";

revoke delete on table "public"."submission_history" from "anon";

revoke insert on table "public"."submission_history" from "anon";

revoke references on table "public"."submission_history" from "anon";

revoke select on table "public"."submission_history" from "anon";

revoke trigger on table "public"."submission_history" from "anon";

revoke truncate on table "public"."submission_history" from "anon";

revoke update on table "public"."submission_history" from "anon";

revoke delete on table "public"."submission_history" from "authenticated";

revoke insert on table "public"."submission_history" from "authenticated";

revoke references on table "public"."submission_history" from "authenticated";

revoke select on table "public"."submission_history" from "authenticated";

revoke trigger on table "public"."submission_history" from "authenticated";

revoke truncate on table "public"."submission_history" from "authenticated";

revoke update on table "public"."submission_history" from "authenticated";

revoke delete on table "public"."submission_history" from "service_role";

revoke insert on table "public"."submission_history" from "service_role";

revoke references on table "public"."submission_history" from "service_role";

revoke select on table "public"."submission_history" from "service_role";

revoke trigger on table "public"."submission_history" from "service_role";

revoke truncate on table "public"."submission_history" from "service_role";

revoke update on table "public"."submission_history" from "service_role";

revoke delete on table "public"."submission_state" from "anon";

revoke insert on table "public"."submission_state" from "anon";

revoke references on table "public"."submission_state" from "anon";

revoke select on table "public"."submission_state" from "anon";

revoke trigger on table "public"."submission_state" from "anon";

revoke truncate on table "public"."submission_state" from "anon";

revoke update on table "public"."submission_state" from "anon";

revoke delete on table "public"."submission_state" from "authenticated";

revoke insert on table "public"."submission_state" from "authenticated";

revoke references on table "public"."submission_state" from "authenticated";

revoke select on table "public"."submission_state" from "authenticated";

revoke trigger on table "public"."submission_state" from "authenticated";

revoke truncate on table "public"."submission_state" from "authenticated";

revoke update on table "public"."submission_state" from "authenticated";

revoke delete on table "public"."submission_state" from "service_role";

revoke insert on table "public"."submission_state" from "service_role";

revoke references on table "public"."submission_state" from "service_role";

revoke select on table "public"."submission_state" from "service_role";

revoke trigger on table "public"."submission_state" from "service_role";

revoke truncate on table "public"."submission_state" from "service_role";

revoke update on table "public"."submission_state" from "service_role";

revoke delete on table "public"."user_passes" from "anon";

revoke insert on table "public"."user_passes" from "anon";

revoke references on table "public"."user_passes" from "anon";

revoke select on table "public"."user_passes" from "anon";

revoke trigger on table "public"."user_passes" from "anon";

revoke truncate on table "public"."user_passes" from "anon";

revoke update on table "public"."user_passes" from "anon";

revoke delete on table "public"."user_passes" from "authenticated";

revoke insert on table "public"."user_passes" from "authenticated";

revoke references on table "public"."user_passes" from "authenticated";

revoke select on table "public"."user_passes" from "authenticated";

revoke trigger on table "public"."user_passes" from "authenticated";

revoke truncate on table "public"."user_passes" from "authenticated";

revoke update on table "public"."user_passes" from "authenticated";

revoke delete on table "public"."user_passes" from "service_role";

revoke insert on table "public"."user_passes" from "service_role";

revoke references on table "public"."user_passes" from "service_role";

revoke select on table "public"."user_passes" from "service_role";

revoke trigger on table "public"."user_passes" from "service_role";

revoke truncate on table "public"."user_passes" from "service_role";

revoke update on table "public"."user_passes" from "service_role";

revoke delete on table "public"."users" from "anon";

revoke insert on table "public"."users" from "anon";

revoke references on table "public"."users" from "anon";

revoke select on table "public"."users" from "anon";

revoke trigger on table "public"."users" from "anon";

revoke truncate on table "public"."users" from "anon";

revoke update on table "public"."users" from "anon";

revoke delete on table "public"."users" from "authenticated";

revoke insert on table "public"."users" from "authenticated";

revoke references on table "public"."users" from "authenticated";

revoke select on table "public"."users" from "authenticated";

revoke trigger on table "public"."users" from "authenticated";

revoke truncate on table "public"."users" from "authenticated";

revoke update on table "public"."users" from "authenticated";

revoke delete on table "public"."users" from "service_role";

revoke insert on table "public"."users" from "service_role";

revoke references on table "public"."users" from "service_role";

revoke select on table "public"."users" from "service_role";

revoke trigger on table "public"."users" from "service_role";

revoke truncate on table "public"."users" from "service_role";

revoke update on table "public"."users" from "service_role";

alter table "public"."ai_task_runs" drop constraint "ai_task_runs_task_type_check";

drop index if exists "public"."uq_ar_pass_open";

alter table "public"."user_passes" alter column "uuid_code" set default lower(((((((encode(extensions.gen_random_bytes(2), 'hex'::text) || '-'::text) || encode(extensions.gen_random_bytes(2), 'hex'::text)) || '-'::text) || encode(extensions.gen_random_bytes(2), 'hex'::text)) || '-'::text) || encode(extensions.gen_random_bytes(2), 'hex'::text)));

alter table "public"."ai_task_runs" add constraint "ai_task_runs_task_type_check" CHECK ((task_type = ANY (ARRAY['classify_standard_emotion'::text, 'summarize_situation_and_journal'::text, 'rolling_digest'::text, 'feedback'::text, 'carryover_digest'::text]))) not valid;

alter table "public"."ai_task_runs" validate constraint "ai_task_runs_task_type_check";

set check_function_bodies = off;

CREATE OR REPLACE FUNCTION public.finalize_carryover_digest(p_user_pass_id uuid, p_emotion_entry_id uuid, p_carryover_digest text, p_gpt_responses jsonb DEFAULT NULL::jsonb)
 RETURNS void
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
begin
  -- 1) pending → done, stats_json에는 carryover_digest만 남김
  update public.analysis_requests
     set status     = 'done',
         updated_at = now(),
         stats_json = jsonb_build_object(
           'carryover_digest', left(coalesce(p_carryover_digest,''), 8000)
         )
   where user_pass_id = p_user_pass_id
     and scope = 'pass'
     and status = 'pending';

  -- 2) carryover 요약 실행 로그 적재
  insert into public.ai_task_runs(
    emotion_entry_id,
    task_type,
    provider,
    model,
    request_id,
    prompt_tokens,
    completion_tokens,
    temperature,
    status,
    created_at,
    updated_at
  )
  values (
    p_emotion_entry_id,
    'carryover_digest',
    coalesce(nullif(p_gpt_responses->>'provider',''), 'openai'),
    coalesce(nullif(p_gpt_responses->>'model',''), 'unknown'),
    nullif(p_gpt_responses->>'request_id',''),
    coalesce((p_gpt_responses->>'prompt_tokens')::int, null),
    coalesce((p_gpt_responses->>'completion_tokens')::int, null),
    coalesce((p_gpt_responses->>'temperature')::numeric, null),
    coalesce(nullif(p_gpt_responses->>'status',''), 'ok'),
    now(),
    now()
  );
end;
$function$
;

CREATE OR REPLACE FUNCTION public.save_feedback_and_finish_v2(p_entry_id uuid, p_sid text, p_feedback_text text, p_gpt_responses jsonb, p_language text DEFAULT 'ko'::text)
 RETURNS TABLE(entry_id uuid, feedback_id uuid, sid text, state_after text, remaining_uses_after integer, updated_at timestamp with time zone, prev_carryover text, current_digest text, is_last_turn boolean)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
DECLARE
  v_now     timestamptz := now();
  v_lang    text := coalesce(nullif(p_language,''), 'ko');
  v_text    text := left(coalesce(p_feedback_text,''), 4000);
  v_feedback_id uuid;
  v_rem_after int;

  -- ▼ 추가: 반환용 임시 변수
  v_prev_carryover text := null;
  v_current_digest text := null;
  v_is_last boolean := false;
BEGIN
  IF length(v_text)=0 THEN
    RAISE EXCEPTION 'feedback_text required';
  END IF;

  WITH
  lock_entry AS (
    -- 엔트리 잠금: 최초 생성 여부/패스 ID 확보
    SELECT e.user_pass_id, coalesce(e.is_feedback_generated,false) AS already_generated
    FROM public.emotion_entries e
    WHERE e.id = p_entry_id
    FOR UPDATE
  ),
  upsert_feedback AS (
    -- 1) 피드백 UPSERT
    INSERT INTO public.emotion_feedbacks(
      id, emotion_entry_id, feedback_text, language,
      created_at, updated_at
    )
    VALUES (
      gen_random_uuid(), p_entry_id, v_text, v_lang,
      v_now, v_now
    )
    ON CONFLICT (emotion_entry_id) DO UPDATE
      SET feedback_text  = excluded.feedback_text,
          language       = excluded.language,
          updated_at     = v_now
    RETURNING id
  ),
  mark_entry AS (
    -- 2) 엔트리 플래그(최초 시각 보존)
    UPDATE public.emotion_entries e
       SET is_feedback_generated = true,
           feedback_generated_at = coalesce(e.feedback_generated_at, v_now)
     WHERE e.id = p_entry_id
    RETURNING 1
  ),
  bump_rollup AS (
    -- 3) 최초 피드백 생성일 때만 롤업 카운트 +1 (멱등)
    INSERT INTO public.pass_rollup_digests AS d
      (user_pass_id, digest_text, last_entry_no, created_at, updated_at)
    SELECT le.user_pass_id,
           coalesce(e.rollup_digest_snapshot, ''),  -- ingest에서 저장해둔 스냅샷 사용
           1, v_now, v_now
    FROM lock_entry le
    JOIN public.emotion_entries e ON e.id = p_entry_id
    WHERE le.already_generated = false
    ON CONFLICT (user_pass_id)
    DO UPDATE SET
      digest_text   = CASE
                        WHEN coalesce(excluded.digest_text,'') = '' THEN d.digest_text
                        ELSE excluded.digest_text
                      END,
      last_entry_no = greatest(d.last_entry_no,0) + 1,
      updated_at    = v_now
    RETURNING 1
  ),
  dec_pass AS (
    -- 4) "처음 생성"인 경우에만 회차 차감
    UPDATE public.user_passes up
       SET remaining_uses = up.remaining_uses - 1,
           first_used_at  = coalesce(first_used_at, v_now),
           updated_at     = v_now
     FROM lock_entry le
     WHERE up.id = le.user_pass_id
       AND le.already_generated = false
    RETURNING up.id AS user_pass_id, up.remaining_uses
  ),
  ar_insert AS (
    -- 마지막 회차면 최종 합본 요약 대기(pending) 요청 1건 생성
    INSERT INTO public.analysis_requests(
      user_pass_id, scope, status, reason, analysis_text,
      stats_json, created_at, updated_at
    )
    SELECT
      d.user_pass_id,
      'pass',
      'pending',
      NULL,
      '',
      jsonb_build_object(
        -- ① 이전 패스 carryover: 1순위 done.stats_json->>'carryover_digest', 폴백 pass_rollup_digests
        'prev_carryover',
          coalesce(prev_ar.carryover_digest, prev_prd.digest_text, ''),
        -- ② 이번 패스 최종 롤링 요약(마지막 회차 시점의 현재본)
        'current_digest',
          coalesce(curr_prd.digest_text, '')
      ),
      v_now, v_now
    FROM dec_pass d
    -- 현재 패스
    LEFT JOIN public.user_passes up ON up.id = d.user_pass_id

    -- ★ 직전 패스 계산: 같은 user_id에서 purchased_at/created_at/id 순서의 바로 이전
    LEFT JOIN LATERAL (
      SELECT prev_id
      FROM (
        SELECT id,
               lag(id) OVER (PARTITION BY user_id ORDER BY purchased_at, created_at, id) AS prev_id
        FROM public.user_passes
        WHERE user_id = up.user_id
      ) x
      WHERE x.id = up.id
    ) prev ON true

    -- 직전 패스의 완료된 분석요청에서 carryover_digest 추출(최신 1건)
    LEFT JOIN LATERAL (
      SELECT ar.stats_json->>'carryover_digest' AS carryover_digest
      FROM public.analysis_requests ar
      WHERE ar.user_pass_id = prev.prev_id
        AND ar.scope = 'pass'
        AND ar.status = 'done'
      ORDER BY ar.updated_at DESC NULLS LAST, ar.created_at DESC
      LIMIT 1
    ) prev_ar ON true

    -- 폴백: 직전 패스의 pass 롤업 다이제스트
    LEFT JOIN public.pass_rollup_digests prev_prd
      ON prev_prd.user_pass_id = prev.prev_id

    -- 이번 패스의 현재 롤업 다이제스트(누적본)
    LEFT JOIN public.pass_rollup_digests curr_prd
      ON curr_prd.user_pass_id = d.user_pass_id

    WHERE d.remaining_uses = 0
    ON CONFLICT (user_pass_id, scope, status) DO NOTHING
    RETURNING stats_json
  ),
  done_state AS (
    -- 5) 상태 종결
    UPDATE public.submission_state s
       SET submit_status = 'done',
           updated_at    = v_now
     WHERE s.sid = p_sid
    RETURNING 1
  )
  -- 최종 remaining_uses_after 계산: 차감이 없었으면 현재값 조회
  SELECT
    (SELECT id FROM upsert_feedback),
    coalesce(
      (SELECT remaining_uses FROM dec_pass LIMIT 1),
      (SELECT up.remaining_uses FROM public.user_passes up
         JOIN lock_entry le ON up.id = le.user_pass_id)
    )
  INTO v_feedback_id, v_rem_after;

  -- ▼ 마지막 회차 여부 플래그 계산
  v_is_last := EXISTS(SELECT 1 FROM dec_pass) AND v_rem_after = 0;

  -- ▼ 마지막 회차라면, 방금 ar_insert에 넣은 재료를 꺼내 반환 변수에 담음
  IF v_is_last THEN
    SELECT
      a.stats_json->>'prev_carryover',
      a.stats_json->>'current_digest'
    INTO
      v_prev_carryover,
      v_current_digest
    FROM ar_insert a
    LIMIT 1;   -- 안전 가드: 멱등/ON CONFLICT로 다중 생성 안 되지만 한 번 더 보장
  END IF;

  -- === GPT 'feedbacks' 로그 적재 (단일 객체 기본, 배열이면 0번만 사용) ===
  BEGIN
    IF p_gpt_responses IS NOT NULL AND (p_gpt_responses ? 'feedbacks') THEN

      -- 배열이면 첫 요소만 꺼내 단일 객체처럼 변환
      IF jsonb_typeof(p_gpt_responses->'feedbacks') = 'array' THEN
        p_gpt_responses := jsonb_set(
          p_gpt_responses,
          '{feedbacks}',
          (p_gpt_responses->'feedbacks')->0
        );
      END IF;

      IF jsonb_typeof(p_gpt_responses->'feedbacks') = 'object' THEN
        INSERT INTO ai_task_runs(
          emotion_entry_id, task_type, provider, model,
          request_id, prompt_tokens, completion_tokens, temperature, status
        )
        VALUES (
          p_entry_id,
          'feedback',
          'openai',
          coalesce(nullif(p_gpt_responses->'feedbacks'->>'model',''), 'unknown'),
          p_gpt_responses->'feedbacks'->>'request_id',
          coalesce(nullif(p_gpt_responses->'feedbacks'->>'prompt_tokens','')::int, 0),
          coalesce(nullif(p_gpt_responses->'feedbacks'->>'completion_tokens','')::int, 0),
          nullif(p_gpt_responses->'feedbacks'->>'temperature','')::numeric,
          CASE lower(coalesce(p_gpt_responses->'feedbacks'->>'status','ok'))
            WHEN 'ok' THEN 'ok'
            WHEN 'fail' THEN 'fail'
            WHEN 'timeout' THEN 'timeout'
            ELSE 'ok'
          END
        );
      END IF;

    END IF;
  EXCEPTION WHEN OTHERS THEN
    -- 로그 적재 실패는 본 흐름에 영향 없게 흡수
    PERFORM public.append_submission_log(p_sid, format('feedbacks log error=%s', SQLSTATE));
  END;
  -- === /GPT 'feedbacks' 로그 적재 ===

  PERFORM public.append_submission_log(p_sid, format('feedback_saved entry=%s', p_entry_id));

  -- ▼ RETURN 채우기
  entry_id := p_entry_id;
  feedback_id := v_feedback_id;
  sid := p_sid;
  state_after := 'done';
  remaining_uses_after := v_rem_after;
  updated_at := v_now;

  -- 추가로 반환: 마지막 회차면 값 채워져 있고, 아니면 NULL / false
  prev_carryover := v_prev_carryover;
  current_digest := v_current_digest;
  is_last_turn := v_is_last;

  RETURN NEXT;
END;
$function$
;

CREATE OR REPLACE FUNCTION public.append_submission_log(p_sid text, p_msg text)
 RETURNS void
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_now text := to_char(now(),'YYYY-MM-DD HH24:MI:SS');
  v_line text := format('%s ts=%s', p_msg, v_now);
begin
  update public.submission_state
   set status_log = right(
                      case when coalesce(status_log,'') = '' then v_line
                           else status_log || ' | ' || v_line end,
                      4000),
       updated_at = now()
  where sid = p_sid;
end;
$function$
;

CREATE OR REPLACE FUNCTION public.clean_visible_text(p_text text)
 RETURNS text
 LANGUAGE sql
 IMMUTABLE
 SET search_path TO 'pg_catalog'
AS $function$
  select trim(
           regexp_replace(
             regexp_replace(coalesce(p_text,''),
               E'[\\u200B\\u200C\\u200D\\uFEFF]', '', 'g'
             ),
             E'\\u00A0', ' ', 'g'
           )
         );
$function$
;

CREATE OR REPLACE FUNCTION public.get_rollup_context(p_user_pass_id uuid, p_limit integer DEFAULT 5)
 RETURNS jsonb
 LANGUAGE sql
 STABLE
 SET search_path TO 'public'
AS $function$with pass_info as (
  -- 패스 메타 정보 (진행도 계산용)
  select
    up.user_id,
    coalesce(up.remaining_uses, 0) as remaining_uses,    -- NULL일 경우 0으로
    p.total_uses,
    -- 다음 엔트리 번호 (최소 1, 최대 total_uses 범위로 보정)
    greatest(1, least(p.total_uses,
      (p.total_uses - coalesce(up.remaining_uses, 0) + 1)
    )) as entry_no_next
  from user_passes up
  join passes p on p.id = up.pass_id
  where up.id = p_user_pass_id
),
prd as (
  -- 롤업 요약(digest)
  select
    coalesce(digest_text, '') as digest_text,      -- NULL일 경우 빈 문자열
    coalesce(last_entry_no, 0) as last_entry_no    -- NULL일 경우 0
  from pass_rollup_digests
  where user_pass_id = p_user_pass_id
),
cnt as (
  -- 전체 엔트리 개수
  select count(*)::int as total_cnt
  from emotion_entries
  where user_pass_id = p_user_pass_id
),
lastn as (
  -- 최근 N개의 엔트리 요약 (표준 감정명까지 조인)
  select
    e.id,
    e.situation_summary_text,
    e.journal_summary_text,
    e.created_at,
    e.standard_emotion_id,
    se.name as standard_emotion_name
  from emotion_entries e
  left join standard_emotions se on se.id = e.standard_emotion_id
  where e.user_pass_id = p_user_pass_id
  and e.is_feedback_generated <> false
  order by e.created_at desc
  limit p_limit
),
lastn_json as (
  -- 최근 N개를 JSON 배열로 변환 (없으면 [] 반환)
  select coalesce(
           (select jsonb_agg(to_jsonb(lastn) order by lastn.created_at desc) from lastn),
           '[]'::jsonb
         ) as arr
)
-- 최종 JSON 반환
select jsonb_build_object(
  -- 진행도 관련
  'user_id',        (select user_id from pass_info),
  'remaining_uses', (select remaining_uses from pass_info),
  'total_uses',     (select total_uses     from pass_info),
  'entry_no_next',  (select entry_no_next  from pass_info),

  -- 요약(digest)
  'digest',         (select digest_text   from prd),
  'last_entry_no',  (select last_entry_no from prd),
  'digest_len',     coalesce(length((select digest_text from prd)), 0),
  'has_digest',     (select exists(select 1 from prd)),

  -- 최근 엔트리
  'recent_summaries', (select arr from lastn_json),
  'recent_count',   coalesce((select total_cnt from cnt), 0),
  'has_recent',     coalesce(((select total_cnt from cnt) > 0), false)
);$function$
;

CREATE OR REPLACE FUNCTION public.ingest_entry_and_rollup(p_sid text, p_user_pass_id uuid, p_user_id uuid, p_entry jsonb, p_gpt_responses jsonb, p_new_digest text, p_ip inet DEFAULT NULL::inet, p_user_agent text DEFAULT NULL::text)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$declare
  v_now           timestamptz := now();
  v_state         text;
  v_linked_id     uuid;
  v_state_pass_id uuid;
  v_entry_id      uuid;
  v_std_id        uuid;
begin
  -- 0) 인자 검증(빠른 실패)
  if p_sid is null or p_user_pass_id is null or p_user_id is null then
    return jsonb_build_object('status','error','reason','missing_sid_or_ids');
  end if;

  -- 1) submission_state 잠금 및 최소 검증 (단 1회 SELECT)
  select submit_status, emotion_entry_id, user_pass_id
    into v_state, v_linked_id, v_state_pass_id
  from public.submission_state
  where sid = p_sid
  for update;

  if v_state is null then
    return jsonb_build_object('status','error','reason','sid_not_found');
  end if;
  if v_linked_id is not null then
    -- 이미 링크되어 있으면 멱등 처리
    return jsonb_build_object('status','skipped','entry_id', v_linked_id);
  end if;
  if v_state <> 'ready' then
    return jsonb_build_object('status','error','reason','bad_state');
  end if;
  if v_state_pass_id is not null and v_state_pass_id <> p_user_pass_id then
    return jsonb_build_object('status','error','reason','pass_mismatch');
  end if;

  -- 2) standard_emotion_id 안전 파싱(실패해도 null 처리)
  if (p_entry ? 'standard_emotion_id')
     and jsonb_typeof(p_entry->'standard_emotion_id')='string'
     and nullif(p_entry->>'standard_emotion_id','') is not null then
    begin
      v_std_id := (p_entry->>'standard_emotion_id')::uuid;
    exception when others then
      v_std_id := null;
    end;
  else
    v_std_id := null;
  end if;

  -- 3) 엔트리 INSERT (status='ready')
  insert into public.emotion_entries(
    id, user_pass_id, user_id,
    raw_emotion_text, supposed_emotion_text,
    standard_emotion_id, standard_emotion_reasoning,
    situation_raw_text, situation_summary_text,
    journal_raw_text,   journal_summary_text,
    emotion_level_label_snapshot,
    feedback_type_label_snapshot,
    feedback_speech_label_snapshot,
    is_feedback_generated,
    created_at, status, error_reason,
    rollup_digest_snapshot
  )
  values (
    gen_random_uuid(),
    p_user_pass_id, p_user_id,
    p_entry->>'raw_emotion',
    nullif(p_entry->>'supposed_emotion',''),
    v_std_id,
    nullif(p_entry->>'standard_emotion_reasoning',''),
    p_entry->>'situation_raw', nullif(p_entry->>'situation_summary',''),
    p_entry->>'journal_raw',   nullif(p_entry->>'journal_summary',''),
    p_entry#>>'{labels,level}',
    p_entry#>>'{labels,feedback_type}',
    p_entry#>>'{labels,speech}',
    false,
    v_now, 'ready', null,
    coalesce(p_new_digest,'')
  )
  returning id into v_entry_id;

  -- === GPT 응답 로그(배열이면 첫 요소만, 아니면 그대로) ===
  declare
    v_cls jsonb;
    v_sum jsonb;
    v_roll jsonb;
  begin
    -- 키가 없거나 null일 수 있으니 안전하게 꺼냄
    v_cls  := case when p_gpt_responses ? 'emotion_standardization'
                    then p_gpt_responses->'emotion_standardization' else null end;
    v_sum  := case when p_gpt_responses ? 'situation_and_journal_summary'
                    then p_gpt_responses->'situation_and_journal_summary' else null end;
    v_roll := case when p_gpt_responses ? 'new_rollup_digest'
                    then p_gpt_responses->'new_rollup_digest' else null end;

    -- 배열이면 0번 요소만 사용해 단일 객체로 표준화
    if v_cls  is not null and jsonb_typeof(v_cls)  = 'array' then v_cls  := v_cls->0;  end if;
    if v_sum  is not null and jsonb_typeof(v_sum)  = 'array' then v_sum  := v_sum->0;  end if;
    if v_roll is not null and jsonb_typeof(v_roll) = 'array' then v_roll := v_roll->0; end if;

    -- 1) 표준단어판단(classify_standard_emotion)
    if v_cls is not null and jsonb_typeof(v_cls) = 'object' then
      insert into ai_task_runs(
        emotion_entry_id, task_type, provider, model,
        request_id, prompt_tokens, completion_tokens, temperature, status
      )
      values (
        v_entry_id,
        'classify_standard_emotion',
        'openai',
        coalesce(nullif(v_cls->>'model',''), 'unknown'),
        v_cls->>'request_id',
        coalesce(nullif(v_cls->>'prompt_tokens','')::int, 0),
        coalesce(nullif(v_cls->>'completion_tokens','')::int, 0),
        nullif(v_cls->>'temperature','')::numeric,
        case lower(coalesce(v_cls->>'status','ok'))
          when 'ok' then 'ok' when 'fail' then 'fail' when 'timeout' then 'timeout' else 'ok'
        end
      );
    end if;

    -- 2) 상황+일기 요약(summarize_situation_and_journal)
    if v_sum is not null and jsonb_typeof(v_sum) = 'object' then
      insert into ai_task_runs(
        emotion_entry_id, task_type, provider, model,
        request_id, prompt_tokens, completion_tokens, temperature, status, summarize_mode
      )
      values (
        v_entry_id,
        'summarize_situation_and_journal',
        'openai',
        coalesce(nullif(v_sum->>'model',''), 'unknown'),
        v_sum->>'request_id',
        coalesce(nullif(v_sum->>'prompt_tokens','')::int, 0),
        coalesce(nullif(v_sum->>'completion_tokens','')::int, 0),
        nullif(v_sum->>'temperature','')::numeric,
        case lower(coalesce(v_sum->>'status','ok'))
          when 'ok' then 'ok' when 'fail' then 'fail' when 'timeout' then 'timeout' else 'ok'
        end,
        v_sum->>'summarize_mode'
      );
    end if;

    -- 3) 롤링 요약(rolling_digest)
    if v_roll is not null and jsonb_typeof(v_roll) = 'object' then
      insert into ai_task_runs(
        emotion_entry_id, task_type, provider, model,
        request_id, prompt_tokens, completion_tokens, temperature, status
      )
      values (
        v_entry_id,
        'rolling_digest',
        'openai',
        coalesce(nullif(v_roll->>'model',''), 'unknown'),
        v_roll->>'request_id',
        coalesce(nullif(v_roll->>'prompt_tokens','')::int, 0),
        coalesce(nullif(v_roll->>'completion_tokens','')::int, 0),
        nullif(v_roll->>'temperature','')::numeric,
        case lower(coalesce(v_roll->>'status','ok'))
          when 'ok' then 'ok' when 'fail' then 'fail' when 'timeout' then 'timeout' else 'ok'
        end
      );
    end if;
  end;
  -- === /GPT 응답 로그 ===

  -- 4) submission_state에 링크만 갱신(상태는 그대로 'ready')
  update public.submission_state
     set emotion_entry_id = v_entry_id,
         user_pass_id     = coalesce(user_pass_id, p_user_pass_id),
         updated_at       = v_now
   where sid = p_sid;

  -- 5) 감사 로그
  perform public.append_submission_log(
    p_sid,
    format('ingest ok entry=%s', v_entry_id)
  );

  return jsonb_build_object('status','ok','entry_id', v_entry_id);
end;$function$
;

CREATE OR REPLACE FUNCTION public.init_validate_and_attach_user(p_sid text, p_uuid_code text, p_required jsonb, p_ip inet DEFAULT NULL::inet, p_user_agent text DEFAULT NULL::text, p_latency_ms integer DEFAULT NULL::integer)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_now         timestamptz := now();
  v_pass        record;
  v_reason      text := null;
  v_missing_key text;
  v_user_id     uuid;
  v_history_id  bigint;

  -- email 처리
  v_email_raw   text;
  v_email_norm  text;
begin
  -- 0) 상태: pending 업서트
  insert into submission_state(sid, uuid_code, submit_status, status_reason, updated_at)
  values (p_sid, p_uuid_code, 'pending', 'received', v_now)
  on conflict (sid) do update
    set uuid_code     = excluded.uuid_code,
        submit_status = 'pending',
        status_reason = 'received',
        updated_at    = v_now;

  -- 1) 필수값 비어있음 체크 (email 포함)
  select key into v_missing_key
  from jsonb_each_text(p_required)
  where normalize_whitespace(value) = ''
  limit 1;

  if v_missing_key is not null then
    v_reason := 'missing_field_'||v_missing_key;
  end if;

  -- 2) pass 유효성 + 잠금
  if v_reason is null then
    select up.* into v_pass
    from user_passes up
    where up.uuid_code = p_uuid_code
    for update;

    if v_pass is null then
      v_reason := 'not_found';
    elsif v_pass.is_active is not true then
      v_reason := 'inactive';
    elsif v_pass.expires_at is not null and v_pass.expires_at <= v_now then
      v_reason := 'expired';
    elsif coalesce(v_pass.remaining_uses,0) <= 0 then
      v_reason := 'no_uses';
    end if;
  end if;

  -- 3) 이메일 정규화/검증 (p_required.email)
  if v_reason is null then
    v_email_raw := coalesce(p_required->>'email','');
    -- 이 함수는 제로폭/비가시 공백 제거 + 트림 + 패턴검사 후 정상일 때 정규화된 이메일 반환, 아니면 NULL 가정
    v_email_norm := normalize_and_validate_email(v_email_raw);
    if v_email_norm is null then
      v_reason := 'invalid_email';
    end if;
  end if;

  -- 4) 실패 공용 처리
  if v_reason is not null then
    update submission_state
       set user_pass_id = case when v_pass is null then null else v_pass.id end,
           uuid_code    = p_uuid_code,
           submit_status= 'fail',
           status_reason= v_reason,
           updated_at   = v_now
     where sid = p_sid;

    perform public.append_submission_log(p_sid, format('init fail reason=%s', v_reason));

    insert into submission_history(
      user_pass_id, uuid_code, result_status, result_reason,
      ip, user_agent, latency_ms, created_at, updated_at
    )
    values (
      case when v_pass is null then null else v_pass.id end,
      p_uuid_code, 'fail', v_reason,
      p_ip, p_user_agent, p_latency_ms, v_now, v_now
    )
    returning id into v_history_id;

    return jsonb_build_object(
      'status','fail',
      'reason', v_reason,
      'history_id', v_history_id
    );
  end if;

  -- 5) 사용자 매핑(UPSERT 스타일)
  if v_pass.user_id is null then
    -- 5-1) 이메일로 사용자 우선 탐색 (검증 완료 메일 우선)
    if v_email_norm is not null and v_email_norm <> '' then
      select id into v_user_id
      from users
      where email_verified = v_email_norm
      order by updated_at desc
      limit 1;

      -- 5-2) 없으면 guest + pending 이메일 재사용
      if v_user_id is null then
        select id into v_user_id
        from users
        where is_guest = true
          and email_pending = v_email_norm
        order by updated_at desc
        limit 1;
      end if;
    end if;

    -- 5-3) 그래도 없으면 새 guest 생성
    if v_user_id is null then
      insert into users(is_guest, email_pending)
      values (true, nullif(v_email_norm,''))
      returning id into v_user_id;
    end if;

    update user_passes
       set user_id = v_user_id
     where uuid_code = p_uuid_code
       and user_id is null;

    v_pass.user_id := v_user_id;
  else
    v_user_id := v_pass.user_id;
    insert into users (id, is_guest, email_pending, updated_at)
    values (v_user_id, true, nullif(v_email_norm,''), v_now)
    on conflict (id) do update
      set email_pending = coalesce(excluded.email_pending, users.email_pending),
          updated_at    = v_now;
  end if;

  -- 6) prev_pass_id 세팅
  update public.user_passes cur
     set prev_pass_id = sub.prev_id,
         updated_at   = v_now
    from (
      select up.id as prev_id
      from public.user_passes up
      where up.user_id = v_user_id
        and up.id <> v_pass.id
        and up.pass_id = v_pass.pass_id
        and up.purchased_at <= v_pass.purchased_at
      order by up.purchased_at desc, up.created_at desc
      limit 1
    ) sub
   where cur.id = v_pass.id
     and (cur.prev_pass_id is distinct from sub.prev_id);

  perform public.append_submission_log(
    p_sid,
    format('prev_linked=%s', coalesce((select prev_pass_id::text from public.user_passes where id=v_pass.id), '-'))
  );

  -- 7) 성공 히스토리
  insert into submission_history(
    user_pass_id, uuid_code, result_status, result_reason,
    ip, user_agent, latency_ms, created_at, updated_at
  )
  values (
    v_pass.id, p_uuid_code, 'pass', 'ok',
    p_ip, p_user_agent, p_latency_ms, v_now, v_now
  )
  returning id into v_history_id;

  -- 8) submission_state: ready 전환
  update submission_state
     set user_pass_id = v_pass.id,
         uuid_code    = p_uuid_code,
         submit_status= 'ready',
         status_reason= 'validation_success',
         updated_at   = v_now
   where sid = p_sid;

  perform public.append_submission_log(p_sid, 'init ok');

  -- 9) 응답
  return jsonb_build_object(
    'status','ok',
    'history_id',       v_history_id,
    'user_pass_id',     v_pass.id,
    'user_id',          v_user_id,
    'uuid_code',        p_uuid_code,
    'remaining_uses',   v_pass.remaining_uses,
    'expires_at',       v_pass.expires_at,
    'is_active',        v_pass.is_active,
    'normalized_email', nullif(v_email_norm,''),
    'normalized_emotion', normalize_whitespace(p_required->>'raw_emotion'),
    'situation_trimmed',  clean_visible_text(p_required->>'situation_raw'),
    'journal_trimmed',    clean_visible_text(p_required->>'journal_raw')
  );
end;
$function$
;

CREATE OR REPLACE FUNCTION public.log_ai_task_run(p_entry_id uuid, p_task_type text, p_model text DEFAULT NULL::text, p_request_id text DEFAULT NULL::text, p_prompt_tokens integer DEFAULT 0, p_completion_tokens integer DEFAULT 0, p_temperature numeric DEFAULT NULL::numeric, p_status text DEFAULT 'ok'::text, p_error_code text DEFAULT NULL::text, p_prompt_version text DEFAULT NULL::text)
 RETURNS void
 LANGUAGE sql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
  insert into ai_task_runs(
    emotion_entry_id, task_type, provider, model,
    request_id, prompt_tokens, completion_tokens, temperature, 
    status, error_code, prompt_version
  )
  values (
    p_entry_id, p_task_type, 'openai', nullif(p_model,''),
    p_request_id, greatest(coalesce(p_prompt_tokens,0),0),
    greatest(coalesce(p_completion_tokens,0),0), p_temperature,
    coalesce(p_status,'ok'),
    nullif(p_error_code,''),
    nullif(p_prompt_version,'')
  );
$function$
;

CREATE OR REPLACE FUNCTION public.mark_submission_fail(p_sid text, p_reason text, p_history_id bigint DEFAULT NULL::bigint, p_error_json jsonb DEFAULT NULL::jsonb, p_ip inet DEFAULT NULL::inet, p_user_agent text DEFAULT NULL::text, p_latency_ms integer DEFAULT NULL::integer, p_emotion_entry_id uuid DEFAULT NULL::uuid)
 RETURNS void
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public', 'pg_temp'
AS $function$
declare
  v_now       timestamptz := now();
  v_user_pass uuid;
  v_uuid_code text;
begin
  update public.submission_state s
     set submit_status = 'fail',
         status_reason = p_reason,
         updated_at    = v_now
   where s.sid = p_sid;

  perform public.append_submission_log(p_sid, format('fail reason=%s', p_reason));

  select s.user_pass_id, s.uuid_code
    into v_user_pass, v_uuid_code
  from public.submission_state s
  where s.sid = p_sid;

  if p_history_id is not null then
    update public.submission_history
       set result_status    = 'error',
           result_reason    = p_reason,
           emotion_entry_id = coalesce(p_emotion_entry_id, emotion_entry_id),
           updated_at       = v_now,
           error_json       = p_error_json
     where id = p_history_id;
  else
    insert into public.submission_history(
      user_pass_id, emotion_entry_id, uuid_code,
      result_status, result_reason, ip, user_agent, latency_ms, created_at, updated_at, error_json
    )
    values (
      v_user_pass, p_emotion_entry_id, v_uuid_code,
      'error', p_reason, p_ip, p_user_agent, p_latency_ms, v_now, v_now, p_error_json
    );
  end if;
end;
$function$
;

CREATE OR REPLACE FUNCTION public.normalize_and_validate_email(p_email text)
 RETURNS text
 LANGUAGE plpgsql
 IMMUTABLE
 SET search_path TO 'pg_catalog'
AS $function$
declare
  v_email  text := lower(btrim(p_email));
  v_local  text;
  v_domain text;
begin
  if v_email is null or v_email = '' then
    return null; -- 빈 입력은 상위 로직에서 허용/불허 결정
  end if;

  -- 숨은 공백/제로폭/비정상 문자 빠르게 차단
  if v_email ~ '[\s\u00A0\u200B\uFEFF<>\(\)\{\}\[\];:"''\\|,`]' then
    return null;
  end if;

  -- 기본 포맷
  if v_email !~ '^[A-Za-z0-9._%+\-]+@[A-Za-z0-9.\-]+\.[A-Za-z]{2,}$' then
    return null;
  end if;

  v_local  := split_part(v_email, '@', 1);
  v_domain := split_part(v_email, '@', 2);

  -- 길이 제한
  if length(v_email) > 320 or length(v_local) > 64 or length(v_domain) > 255 then
    return null;
  end if;

  -- 로컬파트 규칙
  if v_local like '.%' or v_local like '%.'
     or v_local like '%..%' then
    return null;
  end if;

  -- 도메인 규칙
  if v_domain like '-%' or v_domain like '%-' or v_domain like '%..%' then
    return null;
  end if;

  -- ① 플러스 제거(모든 도메인)
  if position('+' in v_local) > 0 then
    v_local := split_part(v_local, '+', 1);
  end if;

  -- ② Gmail 점 제거(도메인 한정)
  if v_domain in ('gmail.com','googlemail.com') then
    v_local := replace(v_local, '.', '');
  end if;

  return v_local || '@' || v_domain;
end;
$function$
;

CREATE OR REPLACE FUNCTION public.normalize_whitespace(p_text text)
 RETURNS text
 LANGUAGE sql
 IMMUTABLE
 SET search_path TO 'pg_catalog'
AS $function$
  select regexp_replace(
           regexp_replace(coalesce(p_text,''),
             E'[\\u00A0\\u200B\\uFEFF]', '', 'g'
           ),
           E'[\\s]+', '', 'g'
         );
$function$
;

CREATE OR REPLACE FUNCTION public.save_feedback_and_finish(p_entry_id uuid, p_sid text, p_feedback_text text, p_gpt_responses jsonb, p_language text DEFAULT 'ko'::text)
 RETURNS TABLE(entry_id uuid, feedback_id uuid, sid text, state_after text, remaining_uses_after integer, updated_at timestamp with time zone)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$declare
  v_now     timestamptz := now();
  v_lang    text := coalesce(nullif(p_language,''), 'ko');
  v_text    text := left(coalesce(p_feedback_text,''), 4000);
  v_feedback_id uuid;
  v_rem_after int;
begin
  if length(v_text)=0 then
    raise exception 'feedback_text required';
  end if;

  with
  lock_entry as (
    -- 엔트리 잠금: 최초 생성 여부/패스 ID 확보
    select e.user_pass_id, coalesce(e.is_feedback_generated,false) as already_generated
    from public.emotion_entries e
    where e.id = p_entry_id
    for update
  ),
  upsert_feedback as (
    -- 1) 피드백 UPSERT
    insert into public.emotion_feedbacks(
      id, emotion_entry_id, feedback_text, language,
      created_at, updated_at
    )
    values (
      gen_random_uuid(), p_entry_id, v_text, v_lang,
      v_now, v_now
    )
    on conflict (emotion_entry_id) do update
      set feedback_text  = excluded.feedback_text,
          language       = excluded.language,
          updated_at     = v_now
    returning id
  ),
  mark_entry as (
    -- 2) 엔트리 플래그(최초 시각 보존)
    update public.emotion_entries e
       set is_feedback_generated = true,
           feedback_generated_at = coalesce(e.feedback_generated_at, v_now)
     where e.id = p_entry_id
    returning 1
  ),
  bump_rollup as (
    -- 3) 최초 피드백 생성일 때만 롤업 카운트 +1 (멱등)
    insert into public.pass_rollup_digests as d
      (user_pass_id, digest_text, last_entry_no, created_at, updated_at)
    select le.user_pass_id,
          coalesce(e.rollup_digest_snapshot, ''),  -- ingest에서 저장해둔 스냅샷 사용
          1, v_now, v_now
    from lock_entry le
    join public.emotion_entries e on e.id = p_entry_id
    where le.already_generated = false
    on conflict (user_pass_id)
    do update set
      digest_text   = case
                        when coalesce(excluded.digest_text,'') = '' then d.digest_text
                        else excluded.digest_text
                      end,
      last_entry_no = greatest(d.last_entry_no,0) + 1,
      updated_at    = v_now
    returning 1
  ),
  dec_pass as (
    -- 4) "처음 생성"인 경우에만 회차 차감
    update public.user_passes up
       set remaining_uses = up.remaining_uses - 1,
           first_used_at  = coalesce(first_used_at, v_now),
           updated_at     = v_now
     from lock_entry le
     where up.id = le.user_pass_id
       and le.already_generated = false
    returning up.id as user_pass_id, up.remaining_uses
  ),
  ar_insert as (
    -- 마지막 회차면 최종 합본 요약 대기(pending) 요청 1건 생성
    insert into public.analysis_requests(
      user_pass_id, scope, status, reason, analysis_text,
      stats_json, created_at, updated_at
    )
    select
      d.user_pass_id,
      'pass',
      'pending',
      null,
      '',
      jsonb_build_object(
        -- ① 이전 패스 carryover: 1순위 done.stats_json->>'carryover_digest', 폴백 pass_rollup_digests
        'prev_carryover',
          coalesce(prev_ar.carryover_digest, prev_prd.digest_text, ''),
        -- ② 이번 패스 최종 롤링 요약(마지막 회차 시점의 현재본)
        'current_digest',
          coalesce(curr_prd.digest_text, '')
      ),
      v_now, v_now
    from dec_pass d
    -- 현재 패스
    left join public.user_passes up on up.id = d.user_pass_id

    -- ★ 직전 패스 계산: 같은 user_id에서 purchased_at/created_at/id 역순으로 바로 앞(id) 1건
    left join lateral (
      select prev_id
      from (
        select id,
              lag(id) over (partition by user_id order by purchased_at, created_at, id) as prev_id
        from public.user_passes
        where user_id = up.user_id
      ) x
      where x.id = up.id
    ) prev on true

    -- 직전 패스의 완료된 분석요청에서 carryover_digest 추출(최신 1건)
    left join lateral (
      select ar.stats_json->>'carryover_digest' as carryover_digest
      from public.analysis_requests ar
      where ar.user_pass_id = prev.prev_id
        and ar.scope = 'pass'
        and ar.status = 'done'
      order by ar.updated_at desc nulls last, ar.created_at desc
      limit 1
    ) prev_ar on true

    -- 폴백: 직전 패스의 pass 롤업 다이제스트
    left join public.pass_rollup_digests prev_prd
      on prev_prd.user_pass_id = prev.prev_id

    -- 이번 패스의 현재 롤업 다이제스트(누적본)
    left join public.pass_rollup_digests curr_prd
      on curr_prd.user_pass_id = d.user_pass_id

    where d.remaining_uses = 0
    on conflict (user_pass_id, scope, status) do nothing
    returning 1
  ),
  done_state as (
    -- 5) 상태 종결
    update public.submission_state s
       set submit_status = 'done',
           updated_at    = v_now
     where s.sid = p_sid
    returning 1
  )
  -- 최종 remaining_uses_after 계산: 차감이 없었으면 현재값 조회
  select
    (select id from upsert_feedback),
    coalesce(
      (select remaining_uses from dec_pass limit 1),
      (select up.remaining_uses from public.user_passes up
         join lock_entry le on up.id = le.user_pass_id)
    )
  into v_feedback_id, v_rem_after;

  -- === GPT 'feedbacks' 로그 적재 (단일 객체 기본, 배열이면 0번만 사용) ===
  begin
    if p_gpt_responses is not null and (p_gpt_responses ? 'feedbacks') then

      -- 배열이면 첫 요소만 꺼내 단일 객체처럼 변환
      if jsonb_typeof(p_gpt_responses->'feedbacks') = 'array' then
        p_gpt_responses := jsonb_set(
          p_gpt_responses,
          '{feedbacks}',
          (p_gpt_responses->'feedbacks')->0
        );
      end if;

      if jsonb_typeof(p_gpt_responses->'feedbacks') = 'object' then
        insert into ai_task_runs(
          emotion_entry_id, task_type, provider, model,
          request_id, prompt_tokens, completion_tokens, temperature, status
        )
        values (
          p_entry_id,
          'feedback',
          'openai',
          coalesce(nullif(p_gpt_responses->'feedbacks'->>'model',''), 'unknown'),
          p_gpt_responses->'feedbacks'->>'request_id',
          coalesce(nullif(p_gpt_responses->'feedbacks'->>'prompt_tokens','')::int, 0),
          coalesce(nullif(p_gpt_responses->'feedbacks'->>'completion_tokens','')::int, 0),
          nullif(p_gpt_responses->'feedbacks'->>'temperature','')::numeric,
          case lower(coalesce(p_gpt_responses->'feedbacks'->>'status','ok'))
            when 'ok' then 'ok'
            when 'fail' then 'fail'
            when 'timeout' then 'timeout'
            else 'ok'
          end
        );
      end if;

    end if;
  exception when others then
    -- 로그 적재 실패는 본 흐름에 영향 없게 흡수
    perform public.append_submission_log(p_sid, format('feedbacks log error=%s', SQLSTATE));
  end;
  -- === /GPT 'feedbacks' 로그 적재 ===

  perform public.append_submission_log(p_sid, format('feedback_saved entry=%s', p_entry_id));

  entry_id := p_entry_id;
  feedback_id := v_feedback_id;
  sid := p_sid;
  state_after := 'done';
  remaining_uses_after := v_rem_after;
  updated_at := v_now;
  return next;
end;$function$
;

CREATE OR REPLACE FUNCTION public.seed_and_record_submission(p_sid text, p_uuid_code text, p_user_pass_id uuid, p_user_id uuid, p_reason text, p_ip inet DEFAULT NULL::inet, p_user_agent text DEFAULT NULL::text, p_latency_ms integer DEFAULT NULL::integer)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$declare
  v_now timestamptz := now();
  v_state text;
  v_status text := 'skipped';
  v_reason text := p_reason;
  v_seed text := '';
  v_seed_source text := 'empty';
  v_prev_pass_id uuid;
begin
  select submit_status into v_state
  from submission_state
  where sid = p_sid;

  if v_state is distinct from 'ready' then
    return jsonb_build_object('status','error','reason','bad_state');
  end if;

  perform 1
  from user_passes
  where id = p_user_pass_id
    and user_id = p_user_id
    and uuid_code = p_uuid_code
    and is_active = true
  for update;

  if not found then
    return jsonb_build_object('status','error','reason','mismatch_or_inactive');
  end if;

  -- 이전 pass에서 시드 소스 탐색
  select up.id into v_prev_pass_id
  from user_passes up
  where up.user_id = p_user_id
    and up.id <> p_user_pass_id
  order by up.created_at desc
  limit 1;

  if v_prev_pass_id is not null then
    select ar.stats_json->>'carryover_digest' into v_seed
    from analysis_requests ar
    where ar.user_pass_id = v_prev_pass_id
      and ar.scope='pass'
      and ar.status='done'
    order by ar.created_at desc
    limit 1;

    if coalesce(v_seed,'') = '' then
      select prd.digest_text into v_seed
      from pass_rollup_digests prd
      where prd.user_pass_id = v_prev_pass_id;

      v_seed_source := case when coalesce(v_seed,'')<>'' then 'prev_pass_digest' else 'empty' end;
    else
      v_seed_source := 'carryover_digest';
    end if;
  end if;
  
  if coalesce(v_seed,'') <> '' then
    insert into pass_rollup_digests(user_pass_id, digest_text, last_entry_no, created_at, updated_at)
    values (p_user_pass_id, v_seed, 0, v_now, v_now)
    on conflict (user_pass_id) do update
      set digest_text = excluded.digest_text,
          updated_at  = v_now;
    v_status := 'seeded';
  else
    v_reason := 'empty_seed';
  end if;

  perform public.append_submission_log(
    p_sid,
    format('seed status=%s seed_source=%s prev_pass_id=%s', v_status, v_seed_source, coalesce(v_prev_pass_id::text,'-'), coalesce(v_reason,'-'))
  );

  return jsonb_build_object(
    'status', v_status,
    'seed_source', v_seed_source,
    'prev_pass_id', v_prev_pass_id,
    'seed_len', coalesce(length(v_seed),0)
  );
end;$function$
;


